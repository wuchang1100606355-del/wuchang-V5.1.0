# 致伺服器端 AI 助手：系統變更與同步說明

**日期**: 2026-01-13
**來源**: 本地端 (Local Agent) / 創辦人授權
**狀態**: 準備同步

我們在本地端進行了以下架構與設定的調整，請在同步時納入考量：

## 1. Google Workspace 配置優化
- **方案調整**: 確認非營利資格已生效。
- **授權策略**: 
  - 僅保留 `admin@wuchang.life` 為 **Business Plus** (進階帳戶)。
  - 其餘使用者 (江爸爸、江媽媽、志工等) 調整為 **Nonprofit (免費版)** 或使用 Odoo Portal 身份，以節省成本。
- **帳號角色**:
  - `jiangbaba@wuchang.life`: 常勝江山管委會主委 (需 Workspace 功能)。
  - `jiangmama@wuchang.life`: 甲抽爸早餐店老闆娘 (需 Workspace 功能)。

## 2. Odoo 多租戶架構設定
已規劃並準備實作以下組織結構：
- **平台層**: 
  - 統一使用 `wuchang.life` 網域與 Odoo 實例。
- **常勝江山管理委員會 (物業範例)**:
  - 獨立公司/組織實體。
  - 管理大樓：仁義街 161 號 (江家)、仁義街 159 巷 2 號 (王家)、第三棟 (保留)。
  - 角色：江爸爸 (主委)。
- **甲抽爸早餐店 (商業範例)**:
  - 獨立公司/組織實體。
  - 角色：江媽媽 (老闆娘)。
- **志工隊**:
  - 江爸爸與江媽媽兼任志工身份，跨組織服務。

## 3. 同步策略
- 本地端已產生相關設定腳本與文件變更。
- 請伺服器端採納本地端的「較新」設定 (Newer Strategy)。
- 系統最高權限 (小 j) 已獲授權執行此架構調整。

## 4. 檔案路徑與權責分工（2026-01-13）

### 4.1 檔案所在位置
- 本地端開發路徑：`C:\wuchang V5.1.0`
- 伺服器端/共享專案路徑（由人類已掛載）：`Z:\`  
  - Z:\ 底下的內容為完整的 `wuchang V5.1.0` 專案樹。

### 4.2 同步設定調整
- 檔案：`config/sync_config.json`
- 重要欄位：
  - `remote_base_path`: 已改為 `Z:\\`
  - `local_base_path`: `C:\\wuchang V5.1.0`
  - `sync_strategy`: `newer`
- 意義：
  - 之後的同步工具在需要「遠端基地端」時，會以 `Z:\` 為主。
  - `C:\wuchang V5.1.0` 主要作為本地開發與備份來源。

### 4.3 本地備份機制
- 本地已建立完整快照備份：
  - 位置：`C:\wuchang V5.1.0\backups\local_snapshots\`
  - 檔名格式：`wuchang_V5.1.0_snapshot_YYYYMMDD_HHMMSS.zip`
- 若伺服器端發現程式碼異常，可在必要時請人類從此快照中還原特定檔案。

### 4.4 伺服器端與本地端角色
- 伺服器端自己負責：
  - 選擇 Python / 虛擬環境 / Docker 版本。
  - 啟動與管理 Odoo、AI 服務及相關守護程式。
- 本地端主要負責：
  - 編輯程式碼與設定，並透過 `Z:\` 或同步工具提供最新版本。
  - 維護上述變更說明檔，讓伺服器端 AI 能了解環境差異。

請伺服器端在讀取本文件後：
- 以 `Z:\` 中的專案內容作為目前「權威版本」來源。
- 將本地備份視為回溯使用，不再依賴舊的 `C:\wuchang V5.1.0` 作為正式運行路徑。
