# 路由器中繼握手配置說明

**配置時間**: 2026-01-07  
**系統版本**: Wuchang OS V5.1.0  
**AI 身份**: Little J (小j)

---

## 📋 概述

已配置路由器中繼機制，實現通過路由器進行外網握手連接。

---

## 🔧 配置詳情

### 路由器信息

- **IP 地址**: 192.168.50.1
- **型號**: RT-BE86U-7428 (ASUS)
- **狀態**: ✅ 在線
- **管理界面**: http://192.168.50.1

### 本地服務信息

- **本地 IP**: 192.168.50.249
- **主機名**: Home-commput.wuchang.life
- **網絡範圍**: 192.168.50.0/24

---

## 🌐 握手端點配置

### 端點列表

| 端點路徑 | 類型 | 說明 | 狀態 |
|---------|------|------|------|
| `/api/handshake` | HTTP | 標準握手端點 | ✅ 已配置 |
| `/api/relay/handshake` | HTTP | 路由器中繼握手 | ✅ 已配置 |
| `/handshake` | HTTP | 簡短握手端點 | ✅ 已配置 |
| `/router/relay/status` | HTTP | 路由器中繼狀態 | ✅ 已配置 |

### 訪問方式

**本地訪問**:
```
http://localhost/api/handshake
http://localhost/api/relay/handshake
http://192.168.50.249/api/handshake
```

**通過路由器**:
```
http://192.168.50.1/relay/api/handshake
```

**外網訪問（通過 Cloudflare 隧道）**:
```
https://wuchang.life/api/handshake
https://wuchang.life/api/relay/handshake
```

---

## 🔄 中繼機制

### 工作流程

```
外網客戶端
    ↓
Cloudflare 隧道 (或路由器端口轉發)
    ↓
路由器 (192.168.50.1) - 中繼節點
    ↓
Caddy 反向代理 (80/443)
    ↓
Odoo 握手控制器
    ↓
響應握手請求
```

### 路由器角色

1. **網絡閘道**: 作為本地網絡的出口
2. **中繼節點**: 轉發外部請求到本地服務
3. **端口轉發**: 可選，用於直接外網訪問（需要公網 IP）

---

## 📡 握手響應格式

### 成功響應

```json
{
  "status": "success",
  "message": "通過路由器中繼握手成功",
  "timestamp": "2026-01-07T...",
  "relay": {
    "enabled": true,
    "router_ip": "192.168.50.1",
    "relayed": true,
    "via_router": true
  },
  "server": {
    "ip": "192.168.50.249",
    "hostname": "Home-commput.wuchang.life",
    "system": "Wuchang OS V5.1.0",
    "ai": "Little J (小j)"
  },
  "client": {
    "ip": "客戶端 IP",
    "forwarded_for": "轉發 IP",
    "via": "Via 頭",
    "user_agent": "User-Agent"
  },
  "endpoints": {
    "command_center": "/command_center",
    "design_report": "/design_report",
    "handshake": "/api/handshake"
  },
  "access": {
    "command_center_url": "http://192.168.50.249/command_center",
    "report_url": "http://192.168.50.249/design_report",
    "access_code": "J2025"
  }
}
```

---

## 🚀 配置方法

### 方法 1: Cloudflare 隧道（推薦）

**優點**:
- ✅ 無需路由器配置
- ✅ 自動穿透 NAT
- ✅ 安全可靠
- ✅ 自動 HTTPS

**配置**:
- 使用現有的 `cloudflared` 服務
- 訪問: `https://wuchang.life/api/handshake`

**狀態**: ✅ 已配置

### 方法 2: 路由器端口轉發

**優點**:
- ✅ 直接訪問，延遲低
- ✅ 不需要第三方服務

**缺點**:
- ⚠️ 需要公網 IP
- ⚠️ 需要配置防火牆
- ⚠️ 安全風險較高

**配置步驟**:

1. **登入路由器管理界面**:
   ```
   http://192.168.50.1
   ```

2. **進入端口轉發設置**:
   - 進階設定 → 外部網絡(WAN) → 虛擬伺服器/端口轉發

3. **添加轉發規則**:
   ```
   服務名稱: Odoo
   外部端口: 8069
   內部 IP: 192.168.50.249
   內部端口: 8069
   協議: TCP
   
   服務名稱: HTTP
   外部端口: 80
   內部 IP: 192.168.50.249
   內部端口: 80
   協議: TCP
   
   服務名稱: HTTPS
   外部端口: 443
   內部 IP: 192.168.50.249
   內部端口: 443
   協議: TCP
   ```

4. **啟用 UPnP**（可選，自動配置）:
   - 進階設定 → 外部網絡(WAN) → 啟用 UPnP

### 方法 3: 路由器反向代理

**配置** (如果在路由器上配置反向代理):
- 路由器作為反向代理服務器
- 將請求轉發到本地服務

---

## 🔒 安全配置

### CORS 設置

握手端點已配置 CORS 支持：
```python
cors='*'  # 允許所有來源（生產環境應限制）
```

### 安全建議

1. **限制 CORS**: 生產環境應限制允許的來源
2. **添加認證**: 可選，添加 API Key 或 Token 驗證
3. **HTTPS 強制**: 外網訪問必須使用 HTTPS
4. **速率限制**: 防止濫用

---

## 📊 測試結果

### 路由器狀態

- ✅ **路由器可訪問**: 192.168.50.1 (HTTP 200)
- ✅ **本地服務**: 192.168.50.249 (在線)

### 握手端點測試

- ⚠️ **本地端點**: 需要 Odoo 服務運行
- ⚠️ **路由器中繼**: 需要配置端口轉發
- ✅ **Cloudflare 隧道**: 推薦方式，無需額外配置

---

## 🔧 故障排查

### 問題 1: 無法訪問握手端點

**可能原因**:
- Odoo 服務未運行
- Caddy 配置未生效
- 端口被防火牆阻止

**解決方法**:
```bash
# 檢查 Odoo 服務
docker ps | grep wuchang-web

# 檢查 Caddy 配置
docker logs caddy

# 重啟 Caddy
docker-compose restart caddy
```

### 問題 2: 路由器中繼失敗

**可能原因**:
- 端口轉發未配置
- 路由器不支持轉發
- 防火牆規則阻止

**解決方法**:
- 檢查路由器端口轉發配置
- 驗證防火牆規則
- 使用 Cloudflare 隧道（推薦）

### 問題 3: 外網無法訪問

**可能原因**:
- Cloudflare 隧道未運行
- 域名未配置
- DNS 解析問題

**解決方法**:
```bash
# 檢查 Cloudflare 隧道
docker logs cloudflared

# 檢查域名解析
nslookup wuchang.life

# 測試 Cloudflare 隧道
curl https://wuchang.life/api/handshake
```

---

## 📝 使用示例

### 通過 curl 測試

```bash
# 本地握手
curl http://localhost/api/handshake

# 通過路由器（如果配置了轉發）
curl http://<路由器公網IP>/api/handshake

# 通過 Cloudflare 隧道
curl https://wuchang.life/api/handshake
```

### 通過 Python 測試

```python
import requests

response = requests.get('https://wuchang.life/api/handshake')
print(response.json())
```

### 通過 JavaScript 測試

```javascript
fetch('https://wuchang.life/api/handshake')
    .then(res => res.json())
    .then(data => console.log(data));
```

---

## ✅ 配置總結

### 已完成的配置

- ✅ 握手控制器已創建 (`handshake_controller.py`)
- ✅ 路由器中繼控制器已創建 (`router_relay.py`)
- ✅ Caddy 路由配置已更新
- ✅ 控制器已註冊到 Odoo
- ✅ CORS 支持已配置

### 待完成的配置

- ⏳ 重啟 Odoo 服務以載入新控制器
- ⏳ 測試握手端點功能
- ⏳ 配置路由器端口轉發（如需要）

---

## 🚀 部署步驟

1. **重啟 Odoo 服務**:
   ```bash
   docker-compose restart wuchang-web
   ```

2. **重啟 Caddy**:
   ```bash
   docker-compose restart caddy
   ```

3. **測試握手**:
   ```bash
   curl http://localhost/api/handshake
   ```

4. **驗證外網訪問**:
   ```bash
   curl https://wuchang.life/api/handshake
   ```

---

**最後更新**: 2026-01-07  
**系統版本**: Wuchang OS V5.1.0  
**AI 身份**: Little J (小j)
