# 下一步操作指南

**Tunnel ID**：`837487dc-1536-43ec-a1c9-ee366a0aa4b7`

---

## ✅ 已完成

1. ✅ 已登入 Cloudflare
2. ✅ 已建立 Tunnel：`wuchang-life`
3. ✅ 已取得 Tunnel ID：`837487dc-1536-43ec-a1c9-ee366a0aa4b7`
4. ✅ 已更新 `config.yml`

---

## 🚀 接下來要做的事

### 步驟 1：複製憑證檔案到容器

**憑證檔案位置**：`C:\Users\o0930\.cloudflared\837487dc-1536-43ec-a1c9-ee366a0aa4b7.json`

**執行以下命令**：

```powershell
# 複製憑證檔案到容器
docker cp "C:\Users\o0930\.cloudflared\837487dc-1536-43ec-a1c9-ee366a0aa4b7.json" wuchangv510-cloudflared-1:/etc/cloudflared/credentials.json

# 複製 config.yml 到容器
docker cp cloudflared/config.yml wuchangv510-cloudflared-1:/etc/cloudflared/config.yml
```

---

### 步驟 2：重啟容器

```powershell
docker restart wuchangv510-cloudflared-1
```

---

### 步驟 3：檢查日誌

```powershell
docker logs wuchangv510-cloudflared-1
```

**應該看到**：
- `Connection established` 或類似的成功訊息
- 沒有錯誤訊息

---

### 步驟 4：在 Cloudflare Dashboard 設定 DNS

**前往**：https://dash.cloudflare.com/70735b2325e4de7daaf806de4f89fcac/wuchang.life/dns/records

**需要修改的記錄**：

#### 記錄 1：根域名（wuchang.life）- 第一個

1. 找到 `wuchang.life` 的 A 記錄（內容是 `220.135.21.74`）
2. 點擊「**編輯**」
3. 修改：
   - **類型**：改為 `CNAME`
   - **目標**：改為 `837487dc-1536-43ec-a1c9-ee366a0aa4b7.cfargotunnel.com`
   - **代理狀態**：保持「通過 Proxy 處理」（橘色雲朵）
4. 點擊「**儲存**」

#### 記錄 2：根域名（wuchang.life）- 第二個

1. 找到 `wuchang.life` 的另一個 A 記錄（內容是 `35.185.167.23`）
2. 點擊「**編輯**」
3. 點擊「**刪除**」
4. 確認刪除

#### 記錄 3：WWW 子域名（www）

1. 找到 `www` 的 A 記錄（內容是 `220.135.21.74`）
2. 點擊「**編輯**」
3. 修改：
   - **類型**：改為 `CNAME`
   - **目標**：改為 `837487dc-1536-43ec-a1c9-ee366a0aa4b7.cfargotunnel.com`
   - **代理狀態**：保持「通過 Proxy 處理」（橘色雲朵）
4. 點擊「**儲存**」

---

## ⏱️ 等待時間

- **DNS 傳播**：約 5-10 分鐘
- **SSL 證書簽發**：約 5-10 分鐘（自動）

---

## ✅ 完成後檢查

### 檢查 DNS 傳播

```powershell
nslookup wuchang.life
nslookup www.wuchang.life
```

應該看到指向 Cloudflare 的 IP。

### 檢查 SSL 證書

在瀏覽器訪問：
- https://wuchang.life
- https://www.wuchang.life

應該看到有效的 SSL 證書（鎖頭圖示）。

---

## 📋 完整命令列表

```powershell
# 1. 複製憑證檔案
docker cp "C:\Users\o0930\.cloudflared\837487dc-1536-43ec-a1c9-ee366a0aa4b7.json" wuchangv510-cloudflared-1:/etc/cloudflared/credentials.json

# 2. 複製 config.yml
docker cp cloudflared/config.yml wuchangv510-cloudflared-1:/etc/cloudflared/config.yml

# 3. 重啟容器
docker restart wuchangv510-cloudflared-1

# 4. 檢查日誌
docker logs wuchangv510-cloudflared-1

# 5. 檢查 DNS（等待 5-10 分鐘後）
nslookup wuchang.life
nslookup www.wuchang.life
```

---

**建立時間**：2026-01-22
