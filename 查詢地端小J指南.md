# 查詢地端小J指南

**更新時間**：2026-01-22

---

## 📋 地端小J基本資訊

### 身份
- **名稱**：地端小 j（Little J）
- **身份**：本地 LLM 助理
- **形象**：白髮小姑娘
- **職責**：系統監控、容器管理、路由器管理、Odoo 模組管理

### 永久權限
1. **路由器管理員權限**（`router_admin`）- 永久有效
2. **內部應用程式模組總管權限**（`odoo_admin`）- 永久有效

---

## 🔍 查詢方式

### 方式 1：透過 UI 查詢

1. **開啟控制中心**
   - 訪問：`http://localhost:8788`
   - 或使用雙螢幕模式開啟

2. **使用小J對話介面**
   - 在控制中心找到「小J對話」區塊
   - 輸入查詢問題
   - 點擊發送

### 方式 2：透過 API 查詢

#### API 端點
```
POST http://localhost:8788/api/local_j/chat
```

#### 請求格式
```json
{
  "message": "你的查詢問題",
  "agent_mode": "little_j",
  "agent_model": "default"
}
```

#### 範例請求
```bash
curl -X POST http://localhost:8788/api/local_j/chat \
  -H "Content-Type: application/json" \
  -d '{
    "message": "查詢系統狀態",
    "agent_mode": "little_j"
  }'
```

#### 回應格式
```json
{
  "ok": true,
  "reply": "小J的回覆",
  "executed": false,
  "intent": "查詢",
  "agent": {
    "mode": "little_j",
    "model": "default",
    "provider": "local"
  }
}
```

---

## 💬 可查詢的內容

### 1. 系統狀態查詢
- 「檢查系統狀態」
- 「查看容器狀態」
- 「檢查服務健康度」
- 「查看網路狀態」

### 2. 路由器查詢
- 「查詢路由器狀態」
- 「查看連線裝置」
- 「檢查網路流量」
- 「查看端口轉發規則」

### 3. Odoo 模組查詢
- 「查詢 Odoo 模組狀態」
- 「查看已安裝模組」
- 「檢查模組依賴」
- 「查看模組配置」

### 4. 容器查詢
- 「查看容器列表」
- 「檢查容器狀態」
- 「查看容器日誌」
- 「檢查容器資源使用」

### 5. DNS 查詢
- 「檢查 DNS 傳播」
- 「查看 DNS 狀態」
- 「載入 DNS 預期值」

### 6. 網路查詢
- 「做網路快照」
- 「查看網路配置」
- 「檢查連線狀態」

### 7. 稽核記錄查詢
- 「查看稽核紀錄」
- 「查詢風險動作記錄」
- 「查看授權記錄」

---

## 🎯 查詢範例

### 範例 1：查詢系統狀態
```json
{
  "message": "檢查系統狀態",
  "agent_mode": "little_j"
}
```

**預期回應**：
- 容器狀態
- 服務健康度
- 系統資源使用
- 網路狀態

### 範例 2：查詢路由器狀態
```json
{
  "message": "查詢路由器狀態和連線裝置",
  "agent_mode": "little_j"
}
```

**預期回應**：
- 路由器連接狀態
- 連線裝置列表
- 網路流量資訊
- 端口轉發規則

### 範例 3：查詢 Odoo 模組
```json
{
  "message": "查看已安裝的 Odoo 模組",
  "agent_mode": "little_j"
}
```

**預期回應**：
- 已安裝模組列表
- 模組狀態
- 模組版本資訊

---

## 🔧 Python 查詢範例

### 使用 requests 查詢
```python
import requests
import json

def query_little_j(message):
    """查詢地端小J"""
    url = "http://localhost:8788/api/local_j/chat"
    payload = {
        "message": message,
        "agent_mode": "little_j"
    }
    
    response = requests.post(url, json=payload)
    if response.status_code == 200:
        data = response.json()
        return data.get("reply", "")
    else:
        return f"查詢失敗: {response.status_code}"

# 使用範例
reply = query_little_j("檢查系統狀態")
print(reply)
```

---

## 📊 地端小J能力

### 監控能力
- ✅ 容器狀態監控（每小時檢查）
- ✅ 系統健康度檢查
- ✅ 網路狀態監控
- ✅ 服務可用性檢查

### 管理能力
- ✅ 路由器管理（永久權限）
- ✅ Odoo 模組管理（永久權限）
- ✅ 容器管理
- ✅ DNS 管理

### 查詢能力
- ✅ 系統狀態查詢
- ✅ 容器狀態查詢
- ✅ 網路狀態查詢
- ✅ 稽核記錄查詢
- ✅ 模組狀態查詢

---

## ⚠️ 注意事項

### 1. 高風險動作
- 地端小J會自動檢查權限
- 高風險動作需要確認
- 遵循「無回應＝禁止高風險作業」原則

### 2. 權限檢查
- 地端小J擁有永久權限：
  - `router_admin`（路由器管理）
  - `odoo_admin`（Odoo 模組管理）
- 其他操作可能需要授權

### 3. 回應時間
- 本地查詢：即時回應
- 需要外部 API：可能延遲
- 複雜查詢：可能需要較長時間

---

## 🚀 快速查詢

### 透過瀏覽器
1. 開啟：`http://localhost:8788`
2. 找到「小J對話」區塊
3. 輸入問題並發送

### 透過命令列
```bash
# 使用 curl
curl -X POST http://localhost:8788/api/local_j/chat \
  -H "Content-Type: application/json" \
  -d '{"message": "檢查系統狀態", "agent_mode": "little_j"}'
```

### 透過 Python
```python
import requests

response = requests.post(
    "http://localhost:8788/api/local_j/chat",
    json={"message": "檢查系統狀態", "agent_mode": "little_j"}
)
print(response.json())
```

---

## 📝 相關檔案

1. **控制中心**：`local_control_center.py`
2. **永久權限配置**：`little_j_permanent_permissions.json`
3. **記憶庫**：`jules_memory_bank.json`
4. **路由器權限授予**：`little_j_router_admin_grant.md`
5. **Odoo 權限授予**：`little_j_odoo_admin_grant.md`

---

**建立時間**：2026-01-22
