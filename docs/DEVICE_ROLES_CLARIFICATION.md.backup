# 設備角色澄清

**文件日期**: 2025-01-07  
**系統版本**: Wuchang OS V5.1.0

---

## 🎯 設備角色定義

### VM 伺服器（核心服務伺服器）
- **IP 地址**: `192.168.50.249`
- **角色**: 被控制端點 / 核心服務伺服器 (Controlled Endpoint / Core Service Server)
- **功能**:
  - 運行 Odoo 核心服務、AI 服務、數據庫
  - 接收來自 UI 設備的指令
  - 提供 API 接口供其他設備調用
  - 在 UI 設備離線時，可作為代理執行部分 UI 工作
- **位置**: 本地機器

### UI 使用者筆電（主機控制端點）
- **IP 地址**: `192.168.50.84`
- **角色**: 主機控制端點 (Primary Control Endpoint)
- **功能**:
  - 負責發送指令、接收狀態、提供使用者介面
  - 可以運行 `sister_agent.py` 或其他控制客戶端
  - 監控和管理其他設備
- **位置**: 使用者筆電設備

### POS 設備（被控制設備）
- **IP 地址**: `192.168.50.86`
- **設備名稱**: `v3_mix_edla_gl`
- **角色**: 被控制設備 (Controlled Device)
- **功能**:
  - 執行 POS 業務邏輯
  - 接收來自 VM 或 UI 設備的指令
  - 回報狀態和數據

---

## 📊 架構圖

```
┌─────────────────────────────────────────┐
│   UI 使用者筆電 (192.168.50.84)         │
│   主機控制端點 (Primary Control)        │
│   - 發送控制指令                        │
│   - 監控設備狀態                        │
└─────────────────────────────────────────┘
              │
              │ 控制指令 / 狀態查詢
              ▼
┌─────────────────────────────────────────┐
│   VM 伺服器 (192.168.50.249)            │
│   核心服務伺服器 (Core Service Server)  │
│   - Odoo 服務                           │
│   - AI 服務                             │
│   - 數據庫                               │
│   - API 接口                            │
└─────────────────────────────────────────┘
              │
              │ 指令 / 數據同步
              ▼
┌─────────────────────────────────────────┐
│   POS 設備 (192.168.50.86)              │
│   v3_mix_edla_gl                        │
│   被控制設備 (Controlled Device)        │
│   - POS 業務邏輯                        │
└─────────────────────────────────────────┘
```

---

## 🔄 通訊流程

1. **UI 設備 → VM 伺服器**
   - UI 設備發送控制指令到 VM (192.168.50.249:8069)
   - VM 執行操作並回報狀態

2. **VM 伺服器 → POS 設備**
   - VM 透過 API 或直接連線控制 POS 設備
   - POS 設備回報狀態和數據到 VM

3. **UI 設備 ← VM 伺服器**
   - VM 將 POS 設備狀態和數據回報給 UI 設備
   - UI 設備顯示和管理

---

## ✅ 確認事項

- [x] VM 伺服器 IP: 192.168.50.249（本地機器）
- [x] UI 設備 IP: 192.168.50.84（使用者筆電）
- [x] POS 設備 IP: 192.168.50.86（v3_mix_edla_gl）
- [x] VM 運行 Odoo 服務（端口 8069）
- [x] UI 設備可訪問 VM 的 Odoo 服務
- [x] POS 設備可訪問 VM 的 Odoo 服務

---

**文件版本**: 1.0  
**最後更新**: 2025-01-07  
**維護者**: 小J (Little J)
