# Google Cloud 成本優化指南

**文件日期**: 2025-01-07  
**系統版本**: Wuchang OS V5.1.0  
**目標**: 優化成本，提升 AI 小J 服務品質

---

## 💰 目前付費項目分析

### 免費服務（必須保留）

| 服務 | 費用 | 狀態 | 說明 |
|------|------|------|------|
| **Maps Embed API** | $0 | ✅ 保留 | 無限制免費 |
| **Geocoding API** | $0 | ✅ 保留 | $200/月免費額度 |

### 需要檢查的付費服務

| 服務 | 預估費用 | 必要性 | 建議 |
|------|---------|--------|------|
| **Vertex AI / Gemini API** | $10-50/月 | ⚠️ 檢查 | 優化使用或使用免費額度 |
| **Cloud Storage** | $5-10/月 | ⚠️ 檢查 | 清理不需要的檔案 |
| **Compute Engine** | $20-50/月 | ⚠️ 檢查 | 如果不需要可停止 |
| **Cloud SQL** | $15-40/月 | ⚠️ 檢查 | 如果不需要可刪除 |

---

## 🔍 檢查步驟

### Step 1: 檢查帳單

1. **訪問帳單頁面**
   - 網址：https://console.cloud.google.com/billing
   - 查看「費用」標籤
   - 查看「依專案分組」的費用明細

2. **識別付費項目**
   - 列出所有產生費用的服務
   - 記錄每個服務的費用
   - 確認是否在免費額度內

### Step 2: 檢查資源使用

1. **VM 執行個體（Compute Engine）**
   - 訪問：https://console.cloud.google.com/compute/instances
   - 檢查是否有運行中的 VM
   - 確認是否必要

2. **儲存空間（Cloud Storage）**
   - 訪問：https://console.cloud.google.com/storage/browser
   - 檢查儲存空間使用量
   - 確認是否有不需要的檔案

3. **資料庫（Cloud SQL）**
   - 訪問：https://console.cloud.google.com/sql/instances
   - 檢查是否有資料庫實例
   - 確認是否必要

### Step 3: 檢查 API 使用

1. **API 使用量**
   - 訪問：https://console.cloud.google.com/apis/dashboard
   - 查看各 API 的使用量
   - 確認是否在免費額度內

2. **Vertex AI / Gemini API**
   - 檢查 API 呼叫次數
   - 檢查費用
   - 確認是否可以使用免費額度

---

## ✅ 優化建議

### 1. AI 服務優化（優先）

#### 使用免費額度

**Google 非營利組織 AI 免費額度**：
- Gemini API：每月有免費額度（需確認）
- Vertex AI：可能有免費額度（需確認）

**優化策略**：
1. **使用較便宜的模型**
   - ✅ `gemini-1.5-flash`（快速、便宜）
   - ⚠️ `gemini-2.5-pro`（較貴，用於重要任務）

2. **優化 API 呼叫**
   - 使用快取減少重複呼叫
   - 批次處理請求
   - 減少不必要的 API 呼叫

3. **本地 AI 方案**
   - 考慮使用 Ollama（本地運行）
   - 減少對雲端 API 的依賴
   - 降低成本

#### 提升服務品質

**在不增加成本的前提下**：
1. **優化提示詞（Prompt）**
   - 更精確的提示詞可以減少 API 呼叫
   - 提高回應品質

2. **使用快取機制**
   - 快取常見問題的回答
   - 減少 API 呼叫

3. **批次處理**
   - 將多個請求合併處理
   - 提高效率

### 2. 關閉不必要的服務

#### Compute Engine（如果不需要）

**檢查**：
- 是否有運行中的 VM？
- VM 是否必要？
- 是否可以停止或刪除？

**操作**：
1. 訪問：https://console.cloud.google.com/compute/instances
2. 選擇不需要的 VM
3. 點擊「停止」或「刪除」
4. 確認操作

**節省**：$20-50/月

#### Cloud Storage（優化）

**檢查**：
- 儲存空間使用量
- 是否有不需要的檔案？
- 可以使用較便宜的儲存類別嗎？

**操作**：
1. 訪問：https://console.cloud.google.com/storage/browser
2. 刪除不需要的檔案
3. 使用「Nearline」或「Coldline」儲存類別（較便宜）

**節省**：$2-5/月

#### Cloud SQL（如果不需要）

**檢查**：
- 是否有資料庫實例？
- 資料庫是否必要？
- 是否可以刪除？

**操作**：
1. 訪問：https://console.cloud.google.com/sql/instances
2. 選擇不需要的資料庫
3. 點擊「刪除」
4. 確認操作（⚠️ 資料會永久刪除）

**節省**：$15-40/月

### 3. 停用不需要的 API

**檢查**：
- 哪些 API 已啟用但未使用？
- 哪些 API 可以停用？

**操作**：
1. 訪問：https://console.cloud.google.com/apis/dashboard
2. 選擇不需要的 API
3. 點擊「停用」
4. 確認操作

---

## 📊 成本優化目標

### 第一階段（立即）

**目標**：關閉不必要的服務，節省 $30-80/月

| 項目 | 行動 | 節省 |
|------|------|------|
| 停止不必要的 VM | 停止或刪除 | $20-50/月 |
| 清理 Cloud Storage | 刪除不需要的檔案 | $2-5/月 |
| 刪除不必要的資料庫 | 刪除不需要的實例 | $15-40/月 |

### 第二階段（優化）

**目標**：優化 AI 服務，提升品質或降低成本

| 項目 | 行動 | 效果 |
|------|------|------|
| 使用免費額度 | 確認並使用 Gemini API 免費額度 | 節省 $10-30/月 |
| 優化 API 呼叫 | 使用快取和批次處理 | 減少 20-30% 呼叫 |
| 使用較便宜的模型 | gemini-1.5-flash | 節省 50-70% 成本 |

### 第三階段（長期）

**目標**：建立成本優化機制

| 項目 | 行動 | 效果 |
|------|------|------|
| 設定預算警報 | $50, $100, $200 | 避免超支 |
| 定期審查 | 每月檢查一次 | 持續優化 |
| 本地 AI 方案 | 考慮 Ollama | 減少雲端依賴 |

---

## 🎯 AI 小J 服務優化

### 提升服務品質（不增加成本）

1. **優化提示詞**
   - 更精確的系統提示詞
   - 減少不必要的 API 呼叫
   - 提高回應品質

2. **使用快取**
   - 快取常見問題的回答
   - 減少重複 API 呼叫
   - 提高回應速度

3. **批次處理**
   - 將多個請求合併
   - 提高效率
   - 降低成本

### 降低成本（維持品質）

1. **使用免費額度**
   - 確認 Gemini API 免費額度
   - 優先使用免費額度
   - 超過後才使用付費

2. **選擇合適的模型**
   - 一般任務：`gemini-1.5-flash`（便宜）
   - 重要任務：`gemini-2.5-pro`（較貴但品質好）

3. **本地 AI 方案**
   - 考慮使用 Ollama
   - 本地運行 AI 模型
   - 減少雲端 API 依賴

---

## 📋 檢查清單

### 立即檢查

- [ ] 檢查帳單，識別付費項目
- [ ] 檢查 VM 執行個體，確認是否必要
- [ ] 檢查 Cloud Storage，清理不需要的檔案
- [ ] 檢查 Cloud SQL，確認是否必要
- [ ] 檢查 API 使用量，確認是否在免費額度內

### 優化行動

- [ ] 停止或刪除不必要的 VM
- [ ] 清理 Cloud Storage 不需要的檔案
- [ ] 刪除不必要的資料庫實例
- [ ] 停用不需要的 API
- [ ] 優化 AI API 使用（快取、批次處理）
- [ ] 確認並使用免費額度

### 長期維護

- [ ] 設定預算警報
- [ ] 每月檢查一次使用量
- [ ] 持續優化成本
- [ ] 考慮本地 AI 方案

---

## 🛠️ 快速操作腳本

### 檢查付費項目

```powershell
.\scripts\check_google_cloud_billing.ps1
```

### 查看詳細指引

訪問：`docs/GOOGLE_CLOUD_COST_OPTIMIZATION.md`

---

## ⚠️ 注意事項

1. **刪除資源前請備份**
   - 確認資料已備份
   - 確認不再需要該資源
   - 刪除後無法恢復

2. **確認免費額度**
   - 確認 Google 非營利組織的免費額度
   - 確認 API 使用是否在免費額度內

3. **定期檢查**
   - 每月檢查一次使用量
   - 設定預算警報
   - 避免意外超支

---

**文件版本**: 1.0  
**最後更新**: 2025-01-07  
**維護者**: 小J (Little J)
