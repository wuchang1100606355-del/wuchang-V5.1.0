# 設備專屬網頁 APP 使用指南

**文件日期**: 2025-01-07  
**系統版本**: Wuchang OS V5.1.0

---

## 🎯 功能概述

為每個納管的設備提供專屬的網頁應用程式，類似移動應用程式的深度連結，包含：

- ✅ **設備專屬端點**：每個設備有唯一的訪問 URL
- ✅ **地理位置定位**：自動取得設備的 GPS 位置
- ✅ **地圖顯示**：在地圖上顯示設備位置
- ✅ **自動更新**：每 5 分鐘自動更新位置
- ✅ **地址解析**：自動將座標轉換為地址

---

## 📱 使用方式

### Step 1: 納管設備

設備納管後，系統會自動生成 `device_token`。

### Step 2: 訪問專屬端點

在設備的瀏覽器中訪問：
```
http://<伺服器地址>/device/<device_token>/app
```

例如：
```
http://192.168.50.249:8069/device/abc123xyz/app
```

### Step 3: 取得地理位置

1. 點擊「📍 取得目前位置」按鈕
2. 允許瀏覽器存取地理位置權限
3. 系統會自動：
   - 取得 GPS 座標（緯度、經度）
   - 在地圖上顯示位置
   - 自動取得地址
   - 儲存到資料庫

---

## 🗺️ 地圖功能

### 地圖服務

- **使用 OpenStreetMap**：免費、開源的地圖服務
- **無需 API Key**：不需要申請任何 API 金鑰
- **離線支援**：可配置離線地圖

### 地圖功能

- ✅ 顯示設備當前位置
- ✅ 標記設備位置
- ✅ 可縮放、拖曳地圖
- ✅ 自動居中顯示設備位置

---

## 📊 位置資訊

### 儲存的資訊

| 欄位 | 說明 | 範例 |
|------|------|------|
| **latitude** | 緯度 | 25.0614 |
| **longitude** | 經度 | 121.4965 |
| **location_address** | 地址 | 新北市三重區... |
| **location_updated** | 更新時間 | 2025-01-07 10:30:00 |

### 自動更新

- **更新頻率**：每 5 分鐘自動更新一次
- **更新方式**：背景自動更新，不影響使用者操作
- **更新條件**：設備必須允許地理位置權限

---

## 🔌 API 端點

### 更新設備位置

**端點**: `POST /device/<device_token>/location`

**請求格式**:
```json
{
  "latitude": 25.0614,
  "longitude": 121.4965,
  "address": "新北市三重區..."
}
```

**回應格式**:
```json
{
  "status": "success",
  "message": "位置已更新",
  "device": {
    "name": "v3_mix_edla_gl",
    "latitude": 25.0614,
    "longitude": 121.4965,
    "address": "新北市三重區...",
    "updated": "2025-01-07T10:30:00"
  }
}
```

### 設備專屬網頁 APP

**端點**: `GET /device/<device_token>/app`

**說明**: 返回設備專屬的網頁應用程式界面

---

## 🎨 界面功能

### 設備資訊卡片

- 顯示設備名稱、類型、IP 地址、狀態
- 即時更新設備狀態

### 地理位置卡片

- **位置資訊**：顯示緯度、經度、地址
- **定位按鈕**：手動觸發定位
- **地圖顯示**：即時顯示設備位置

### 快速功能

根據設備類型顯示不同的快速功能：
- **POS 設備**：POS 系統連結
- **Chrome OS 設備**：客戶顯示連結
- **所有設備**：設備狀態查詢

---

## 🔒 安全性

### Token 機制

- 每個設備有唯一的 `device_token`
- Token 自動生成，無法猜測
- Token 用於識別設備身份

### 權限控制

- 公開端點（`auth='public'`）
- 僅能更新自己的設備位置
- 無法修改其他設備資訊

---

## 📱 行動裝置支援

### 響應式設計

- ✅ 支援手機、平板、桌面電腦
- ✅ 自動適應螢幕大小
- ✅ 觸控友善的界面

### 瀏覽器相容性

- ✅ Chrome/Edge（推薦）
- ✅ Safari
- ✅ Firefox
- ✅ 行動瀏覽器

---

## 💡 使用場景

### 1. POS 設備定位

- 追蹤 POS 設備的實際位置
- 確認設備是否在正確的店面位置
- 設備遺失時可追蹤位置

### 2. 客戶顯示器管理

- 確認客戶顯示器的安裝位置
- 追蹤移動式顯示器的位置

### 3. 設備管理

- 在地圖上查看所有設備的位置
- 快速找到特定設備
- 規劃設備部署位置

---

## 🛠️ 技術細節

### 地理位置 API

- **標準**: W3C Geolocation API
- **精度**: 高精度模式（GPS）
- **超時**: 10 秒
- **快取**: 不使用快取，每次都取得最新位置

### 地圖服務

- **服務**: OpenStreetMap
- **嵌入方式**: iframe
- **標記**: 自動標記設備位置
- **縮放**: 自動調整縮放級別

### 反向地理編碼

- **服務**: Nominatim (OpenStreetMap)
- **用途**: 將座標轉換為地址
- **格式**: JSON
- **限制**: 需遵守使用條款

---

## 📋 檢查清單

- [ ] 設備已納管並有 `device_token`
- [ ] 瀏覽器支援 Geolocation API
- [ ] 已允許地理位置權限
- [ ] 網路連線正常
- [ ] 可以訪問設備專屬端點

---

## 🐛 故障排除

### 無法取得位置

1. **檢查瀏覽器權限**
   - 確認已允許地理位置權限
   - 清除瀏覽器快取後重試

2. **檢查設備設定**
   - GPS 是否開啟
   - 定位服務是否啟用

3. **檢查網路**
   - 確認可以連接到伺服器
   - 確認可以訪問地圖服務

### 地圖無法顯示

1. **檢查網路連線**
   - 確認可以訪問 OpenStreetMap
   - 檢查防火牆設定

2. **檢查瀏覽器**
   - 嘗試使用其他瀏覽器
   - 清除瀏覽器快取

---

**文件版本**: 1.0  
**最後更新**: 2025-01-07  
**維護者**: 小J (Little J)
